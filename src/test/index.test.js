const fetchShips = require('../index');

describe('fetchShips', () => {
  it('exists', () => {
    expect(fetchShips).toBeDefined();
  });

  it('exists', () => {
    expect(fetchShips).toBeDefined();
  });
});
